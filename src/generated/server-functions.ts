// SPDX-License-Identifier: MIT
// Copyright (C) 2026 Shogo Technologies, Inc.
/**
 * Auto-generated Client Functions
 *
 * Fetch-based CRUD operations that call the Hono REST API.
 * No server-side imports — safe for browser bundles.
 * Generated by `shogo generate` - DO NOT EDIT
 */

import type {
  UserType,
  UserCreateInput,
  UserUpdateInput,
  MenuCategoryType,
  MenuCategoryCreateInput,
  MenuCategoryUpdateInput,
  MenuItemType,
  MenuItemCreateInput,
  MenuItemUpdateInput,
  TableType,
  TableCreateInput,
  TableUpdateInput,
  OrderType,
  OrderCreateInput,
  OrderUpdateInput,
  OrderItemType,
  OrderItemCreateInput,
  OrderItemUpdateInput,
  InventoryItemType,
  InventoryItemCreateInput,
  InventoryItemUpdateInput,
  InventoryTransactionType,
  InventoryTransactionCreateInput,
  InventoryTransactionUpdateInput,
  DailyReportType,
  DailyReportCreateInput,
  DailyReportUpdateInput,
} from './types'

/** Get the API base URL */
function getApiBase(): string {
  if (typeof window !== 'undefined') {
    return window.location.origin
  }
  return process.env.API_URL || 'http://localhost:3001'
}

// ============================================================================
// User Client Functions
// ============================================================================

/**
 * List all User records
 */
export async function getUserList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<UserType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/users${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list User')
  }
  const json = await response.json()
  return (json.items || []) as UserType[]
}

/**
 * Get a single User by ID
 */
export async function getUserById(args: { data: { id: string; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'User not found')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Create a new User
 */
export async function createUser(args: { data: { input: UserCreateInput; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create User')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Update an existing User
 */
export async function updateUser(args: { data: { id: string; input: UserUpdateInput; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update User')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Delete a User
 */
export async function deleteUser(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete User')
  }
  return { success: true }
}

// ============================================================================
// MenuCategory Client Functions
// ============================================================================

/**
 * List all MenuCategory records
 */
export async function getMenuCategoryList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<MenuCategoryType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/menu-categories${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list MenuCategory')
  }
  const json = await response.json()
  return (json.items || []) as MenuCategoryType[]
}

/**
 * Get a single MenuCategory by ID
 */
export async function getMenuCategoryById(args: { data: { id: string; userId?: string } }): Promise<MenuCategoryType> {
  const url = `${getApiBase()}/api/menu-categories/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'MenuCategory not found')
  }
  const json = await response.json()
  return json.data as MenuCategoryType
}

/**
 * Create a new MenuCategory
 */
export async function createMenuCategory(args: { data: { input: MenuCategoryCreateInput; userId?: string } }): Promise<MenuCategoryType> {
  const url = `${getApiBase()}/api/menu-categories`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create MenuCategory')
  }
  const json = await response.json()
  return json.data as MenuCategoryType
}

/**
 * Update an existing MenuCategory
 */
export async function updateMenuCategory(args: { data: { id: string; input: MenuCategoryUpdateInput; userId?: string } }): Promise<MenuCategoryType> {
  const url = `${getApiBase()}/api/menu-categories/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update MenuCategory')
  }
  const json = await response.json()
  return json.data as MenuCategoryType
}

/**
 * Delete a MenuCategory
 */
export async function deleteMenuCategory(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/menu-categories/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete MenuCategory')
  }
  return { success: true }
}

// ============================================================================
// MenuItem Client Functions
// ============================================================================

/**
 * List all MenuItem records
 */
export async function getMenuItemList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<MenuItemType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/menu-items${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list MenuItem')
  }
  const json = await response.json()
  return (json.items || []) as MenuItemType[]
}

/**
 * Get a single MenuItem by ID
 */
export async function getMenuItemById(args: { data: { id: string; userId?: string } }): Promise<MenuItemType> {
  const url = `${getApiBase()}/api/menu-items/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'MenuItem not found')
  }
  const json = await response.json()
  return json.data as MenuItemType
}

/**
 * Create a new MenuItem
 */
export async function createMenuItem(args: { data: { input: MenuItemCreateInput; userId?: string } }): Promise<MenuItemType> {
  const url = `${getApiBase()}/api/menu-items`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create MenuItem')
  }
  const json = await response.json()
  return json.data as MenuItemType
}

/**
 * Update an existing MenuItem
 */
export async function updateMenuItem(args: { data: { id: string; input: MenuItemUpdateInput; userId?: string } }): Promise<MenuItemType> {
  const url = `${getApiBase()}/api/menu-items/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update MenuItem')
  }
  const json = await response.json()
  return json.data as MenuItemType
}

/**
 * Delete a MenuItem
 */
export async function deleteMenuItem(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/menu-items/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete MenuItem')
  }
  return { success: true }
}

// ============================================================================
// Table Client Functions
// ============================================================================

/**
 * List all Table records
 */
export async function getTableList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<TableType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/tables${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Table')
  }
  const json = await response.json()
  return (json.items || []) as TableType[]
}

/**
 * Get a single Table by ID
 */
export async function getTableById(args: { data: { id: string; userId?: string } }): Promise<TableType> {
  const url = `${getApiBase()}/api/tables/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Table not found')
  }
  const json = await response.json()
  return json.data as TableType
}

/**
 * Create a new Table
 */
export async function createTable(args: { data: { input: TableCreateInput; userId?: string } }): Promise<TableType> {
  const url = `${getApiBase()}/api/tables`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Table')
  }
  const json = await response.json()
  return json.data as TableType
}

/**
 * Update an existing Table
 */
export async function updateTable(args: { data: { id: string; input: TableUpdateInput; userId?: string } }): Promise<TableType> {
  const url = `${getApiBase()}/api/tables/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Table')
  }
  const json = await response.json()
  return json.data as TableType
}

/**
 * Delete a Table
 */
export async function deleteTable(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/tables/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Table')
  }
  return { success: true }
}

// ============================================================================
// Order Client Functions
// ============================================================================

/**
 * List all Order records
 */
export async function getOrderList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<OrderType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/orders${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Order')
  }
  const json = await response.json()
  return (json.items || []) as OrderType[]
}

/**
 * Get a single Order by ID
 */
export async function getOrderById(args: { data: { id: string; userId?: string } }): Promise<OrderType> {
  const url = `${getApiBase()}/api/orders/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Order not found')
  }
  const json = await response.json()
  return json.data as OrderType
}

/**
 * Create a new Order
 */
export async function createOrder(args: { data: { input: OrderCreateInput; userId?: string } }): Promise<OrderType> {
  const url = `${getApiBase()}/api/orders`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Order')
  }
  const json = await response.json()
  return json.data as OrderType
}

/**
 * Update an existing Order
 */
export async function updateOrder(args: { data: { id: string; input: OrderUpdateInput; userId?: string } }): Promise<OrderType> {
  const url = `${getApiBase()}/api/orders/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Order')
  }
  const json = await response.json()
  return json.data as OrderType
}

/**
 * Delete a Order
 */
export async function deleteOrder(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/orders/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Order')
  }
  return { success: true }
}

// ============================================================================
// OrderItem Client Functions
// ============================================================================

/**
 * List all OrderItem records
 */
export async function getOrderItemList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<OrderItemType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/order-items${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list OrderItem')
  }
  const json = await response.json()
  return (json.items || []) as OrderItemType[]
}

/**
 * Get a single OrderItem by ID
 */
export async function getOrderItemById(args: { data: { id: string; userId?: string } }): Promise<OrderItemType> {
  const url = `${getApiBase()}/api/order-items/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'OrderItem not found')
  }
  const json = await response.json()
  return json.data as OrderItemType
}

/**
 * Create a new OrderItem
 */
export async function createOrderItem(args: { data: { input: OrderItemCreateInput; userId?: string } }): Promise<OrderItemType> {
  const url = `${getApiBase()}/api/order-items`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create OrderItem')
  }
  const json = await response.json()
  return json.data as OrderItemType
}

/**
 * Update an existing OrderItem
 */
export async function updateOrderItem(args: { data: { id: string; input: OrderItemUpdateInput; userId?: string } }): Promise<OrderItemType> {
  const url = `${getApiBase()}/api/order-items/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update OrderItem')
  }
  const json = await response.json()
  return json.data as OrderItemType
}

/**
 * Delete a OrderItem
 */
export async function deleteOrderItem(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/order-items/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete OrderItem')
  }
  return { success: true }
}

// ============================================================================
// InventoryItem Client Functions
// ============================================================================

/**
 * List all InventoryItem records
 */
export async function getInventoryItemList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<InventoryItemType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/inventory-items${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list InventoryItem')
  }
  const json = await response.json()
  return (json.items || []) as InventoryItemType[]
}

/**
 * Get a single InventoryItem by ID
 */
export async function getInventoryItemById(args: { data: { id: string; userId?: string } }): Promise<InventoryItemType> {
  const url = `${getApiBase()}/api/inventory-items/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'InventoryItem not found')
  }
  const json = await response.json()
  return json.data as InventoryItemType
}

/**
 * Create a new InventoryItem
 */
export async function createInventoryItem(args: { data: { input: InventoryItemCreateInput; userId?: string } }): Promise<InventoryItemType> {
  const url = `${getApiBase()}/api/inventory-items`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create InventoryItem')
  }
  const json = await response.json()
  return json.data as InventoryItemType
}

/**
 * Update an existing InventoryItem
 */
export async function updateInventoryItem(args: { data: { id: string; input: InventoryItemUpdateInput; userId?: string } }): Promise<InventoryItemType> {
  const url = `${getApiBase()}/api/inventory-items/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update InventoryItem')
  }
  const json = await response.json()
  return json.data as InventoryItemType
}

/**
 * Delete a InventoryItem
 */
export async function deleteInventoryItem(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/inventory-items/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete InventoryItem')
  }
  return { success: true }
}

// ============================================================================
// InventoryTransaction Client Functions
// ============================================================================

/**
 * List all InventoryTransaction records
 */
export async function getInventoryTransactionList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<InventoryTransactionType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/inventory-transactions${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list InventoryTransaction')
  }
  const json = await response.json()
  return (json.items || []) as InventoryTransactionType[]
}

/**
 * Get a single InventoryTransaction by ID
 */
export async function getInventoryTransactionById(args: { data: { id: string; userId?: string } }): Promise<InventoryTransactionType> {
  const url = `${getApiBase()}/api/inventory-transactions/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'InventoryTransaction not found')
  }
  const json = await response.json()
  return json.data as InventoryTransactionType
}

/**
 * Create a new InventoryTransaction
 */
export async function createInventoryTransaction(args: { data: { input: InventoryTransactionCreateInput; userId?: string } }): Promise<InventoryTransactionType> {
  const url = `${getApiBase()}/api/inventory-transactions`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create InventoryTransaction')
  }
  const json = await response.json()
  return json.data as InventoryTransactionType
}

/**
 * Update an existing InventoryTransaction
 */
export async function updateInventoryTransaction(args: { data: { id: string; input: InventoryTransactionUpdateInput; userId?: string } }): Promise<InventoryTransactionType> {
  const url = `${getApiBase()}/api/inventory-transactions/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update InventoryTransaction')
  }
  const json = await response.json()
  return json.data as InventoryTransactionType
}

/**
 * Delete a InventoryTransaction
 */
export async function deleteInventoryTransaction(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/inventory-transactions/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete InventoryTransaction')
  }
  return { success: true }
}

// ============================================================================
// DailyReport Client Functions
// ============================================================================

/**
 * List all DailyReport records
 */
export async function getDailyReportList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<DailyReportType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/daily-reports${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list DailyReport')
  }
  const json = await response.json()
  return (json.items || []) as DailyReportType[]
}

/**
 * Get a single DailyReport by ID
 */
export async function getDailyReportById(args: { data: { id: string; userId?: string } }): Promise<DailyReportType> {
  const url = `${getApiBase()}/api/daily-reports/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'DailyReport not found')
  }
  const json = await response.json()
  return json.data as DailyReportType
}

/**
 * Create a new DailyReport
 */
export async function createDailyReport(args: { data: { input: DailyReportCreateInput; userId?: string } }): Promise<DailyReportType> {
  const url = `${getApiBase()}/api/daily-reports`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create DailyReport')
  }
  const json = await response.json()
  return json.data as DailyReportType
}

/**
 * Update an existing DailyReport
 */
export async function updateDailyReport(args: { data: { id: string; input: DailyReportUpdateInput; userId?: string } }): Promise<DailyReportType> {
  const url = `${getApiBase()}/api/daily-reports/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update DailyReport')
  }
  const json = await response.json()
  return json.data as DailyReportType
}

/**
 * Delete a DailyReport
 */
export async function deleteDailyReport(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/daily-reports/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete DailyReport')
  }
  return { success: true }
}
